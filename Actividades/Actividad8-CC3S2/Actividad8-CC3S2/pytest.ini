[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    smoke: marks tests as smoke tests (quick critical functionality)
    regression: marks tests as regression tests (extended test suite)


